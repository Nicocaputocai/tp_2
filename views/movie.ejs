<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pelicula</title>
</head>
<body>
    <%- include('partials/header') %> 
    <h1><%= pelicula.title %> </h1>
    <ul>
        <li>Duracion: <%= pelicula.length %>  </li>
        <li>Se estrenó: <%= pelicula.release_date %>  </li>
        <li> Premios: <%= pelicula.awards %>  </li>
        <li> Rating: <%= pelicula.rating %>  </li>
        <% if (pelicula.Genres) { %>
        <li>Genero: <%= pelicula.Genres.name %> </li>
        <% } %>
    </ul>
    
    <form action="/movies/delete/<%= pelicula.id %>?_method=DELETE" method="POST">
    <button type="submit"> Eliminar </button>
    </form>
</body>
</html>